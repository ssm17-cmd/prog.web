let numeroAleatorio = Math.random() * 50;
let numeroInteiro = Math.floor(numeroAleatorio);
let numeroSecreto = numeroInteiro + 1;
let sugestao;

do {
  sugestao = prompt("Digite um número de 1 a 50:");
  sugestao = parseInt(sugestao);

  if (palpite < numeroSecreto) {
    alert("maior");
  } else if (sugestao > numeroSecreto) {
    alert("menor");
  } else {
    alert("Parabéns! Você acertou.");
  }
} while (sugestao != numeroSecreto);
